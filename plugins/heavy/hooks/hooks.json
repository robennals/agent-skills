{
  "description": "heavy — denies expensive Bash commands that are not wrapped, and keeps the PATH symlink pointing at the installed version",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/heavy-guard.js\"",
            "timeout": 10
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/hooks/ensure-path.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
